# 安全与数据说明

当前版本面向个人本机使用，尚未完成正式安全审计或公开部署验收。

## 登录和授权

GitHub 密码只应输入在 GitHub 官方页面。本应用使用 OAuth，默认请求 `read:user` 与 `user:email`，不申请 Star/Follow 或仓库内容写入权限。登录受稳定账号 ID 允许名单限制。

访问令牌和刷新令牌在 PostgreSQL 中加密保存，密钥来自 `BETTER_AUTH_SECRET`。不要在保留数据库的情况下随意更换此密钥，否则旧凭据无法解密；数据库与密钥同时泄露时，加密无法继续提供有效保护。客户端使用 HttpOnly / SameSite 会话 Cookie，业务写请求校验 Origin。

访问令牌按需续期，不等于永久授权。撤销访问请在 GitHub Settings → Applications 找到对应应用并 Revoke；退出网页登录通常只结束网页会话，不撤销 GitHub 授权。已经收录的内容和阅读记录不会因撤销授权而自动删除。

## 数据流

应用保存公开项目/博主更新、订阅、阅读偏好与 AI 结果。启用 AI 后，相关公开正文会发送给配置的模型服务；默认关闭真实模型。预算是应用估算，不等于供应商账单或硬额度。

服务端配置的 `GITHUB_READ_TOKEN` 会覆盖账号读取凭据，应仅使用足够读取公开信息的权限，不要填写拥有不必要写权限的令牌。

## 本地和公开运行

Compose 数据库和默认 Web 只绑定 `127.0.0.1`。演示密码仅用于本地，`APP_MODE=demo` 不是可公开部署的认证方案。对外运行需要 HTTPS、独立数据库账户、生产随机密钥、备份与恢复验证；本仓库没有交付这些部署设施。

`.env*`（除示例）、构建目录、运行日志、测试产物和数据库导出不得提交。演示数据不是私人账号数据，但截图和日志仍需检查再公开。

如报告安全问题，请勿在公开 Issue、PR 或截图中附上令牌、密码、Cookie、数据库导出或可用于访问个人服务的信息。涉及有效凭据时应先在对应服务撤销/轮换，随后提供脱敏的复现说明。
